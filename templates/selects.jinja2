<p>When uploading a book, please make sure it's named similar to: "Logston, Anne - Shadow"</p>
<form action="/" method="post" enctype="multipart/form-data" class="form-inline" role="form">
  <div class="form-group">
    <select class="selectpicker" id="authorList" onchange="authorGo()" data-live-search="true" data-style="btn-default" data-size="9">
      <option>Find author</option>
      {% for author in authors %}
        <option value="{{ author.split()[-1] }}">{{ author.split()[0:-1]|join(' ') }}</option>
      {% endfor %}
    </select>
  </div>
  <div class="form-group">
    <select class="selectpicker" id="titleList" onchange="titleGo()" data-live-search="true" data-style="btn-primary" data-size="9">
      <option>Find book</option>
      {% for title in titles -%}
        <option value="{{ title.split()[-1] }}">{{ title.split()[0:-1]|join(' ') }}</option>
      {%- endfor %}
    </select>
  </div>
  <div class="form-group">
    <label class="sr-only" for="upload">File to upload</label>
    <input type="file" name="file" id="upload">
  </div>
  <button type="submit" class="btn btn-default">Upload book</button>
</form>
